apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80          # Port the service exposes inside the cluster
      targetPort: 8080  # Port your Node.js app is actually listening on
  type: LoadBalancer